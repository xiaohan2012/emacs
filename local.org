* file opening
#+begin_src emacs-lisp
  (defun my/open-dairy ()
    (interactive)
    (find-file "~/docs/notes/dairy2026.org"))
  (global-set-key (kbd "C-c o r") 'my/open-dairy)

  (defun my/open-upright ()
    (interactive)
    (find-file "~/docs/notes/upright.org"))
  (global-set-key (kbd "C-c o u") 'my/open-upright)

  (defun my/open-org-songs ()
    (interactive)
    (find-file "~/docs/notes/songs.org"))
  (global-set-key (kbd "C-c o s") 'my/open-org-songs)

    (defun my/open-immune-gnn ()
    (interactive)
    (find-file "~/docs/notes/immune-gnn.org"))
  (global-set-key (kbd "C-c o g") 'my/open-immune-gnn)  
#+end_src

#+RESULTS:
: my/open-immune-gnn

* clipboard strings

#+begin_src emacs-lisp
    (defun my/copy-phone-number ()
      (interactive)
      (kill-new "0442483528")
      (message "copid '0442483528'")
      )

    (defun my/copy-nordea-bank-account ()
      (interactive)
      (kill-new "FI66 1470 3500 7401 16")
      (message "copid 'FI66 1470 3500 7401 16'")
      )

    (defun my/copy-address ()
      (interactive)
      (kill-new "Gummeruksenkatu 3 B 22, 40100 Jyväskylä")
      (message "copied 'Gummeruksenkatu 3 B 22, 40100 Jyväskylä'")
      )


    (defun my/copy-finnair-plus-number ()
      (interactive)
      (kill-new "693396855")
      (message "copied '693396855'")
      )  

    (defun my/copy-fudan-ssh-pwd ()
      (interactive)
      (kill-new "Qty062015")
      (message "copied 'Qty062015'")
      )  

    (defun my/copy-linkedin-url ()
      (interactive)
      (kill-new "https://www.linkedin.com/in/han-xiao-7499a840/")
      (message "copied 'https://www.linkedin.com/in/han-xiao-7499a840/'")
      )  

  (defun my/copy-github-url ()
    (interactive)
    (kill-new "https://github.com/xiaohan2012")
    (message "copied 'https://github.com/xiaohan2012'")
    )    

#+end_src



#+RESULTS:
: my/copy-github-url

